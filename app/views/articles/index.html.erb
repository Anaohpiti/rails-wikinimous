<h1>Our great articles</h1>
<ul>
  <% @articles.each do |article|  %>
    <li>📰<%= link_to article.title, article_path(article)%></li>
    <%= link_to "🗑️ - Delete",
                article_path(article),
                method: :delete,
                data: { confirm: "Are you sure?" } %>
  <% end %>
</ul>
<%=link_to "Create an article", new_article_path%>
